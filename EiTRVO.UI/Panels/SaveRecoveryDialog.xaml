<Window x:Class="EiTRVO.UI.Panels.SaveRecoveryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="恢复存档密钥" MinHeight="200" Height="280" Width="450"
        FontFamily="{StaticResource CustomFont}"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBg}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            <!-- Title -->
            <TextBlock Text="恢复存档密钥"
                       FontSize="15" FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimary}"
                       Margin="0,0,0,12"/>

            <TextBlock Text="选择恢复方式:"
                       Foreground="{DynamicResource TextSecondary}" FontSize="12"
                       Margin="0,0,0,8"/>

            <!-- .savkey file recovery -->
            <RadioButton x:Name="rbKeyBackup" GroupName="RecoveryMethod"
                         IsChecked="True" Margin="0,4">
                <StackPanel>
                    <TextBlock Text="从密钥备份文件恢复"
                               FontWeight="SemiBold" FontSize="13"
                               Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock Text="选择之前导出的 .savkey 文件"
                               FontSize="11" Foreground="{DynamicResource TextMuted}"/>
                </StackPanel>
            </RadioButton>

            <!-- .savrec file recovery -->
            <RadioButton x:Name="rbRecoveryFile" GroupName="RecoveryMethod"
                         Margin="0,10">
                <StackPanel>
                    <TextBlock Text="从恢复文件恢复 (.savrec)"
                               FontWeight="SemiBold" FontSize="13"
                               Foreground="{DynamicResource TextPrimary}"/>
                    <TextBlock x:Name="lblMsAccount" FontSize="11"
                               Foreground="{DynamicResource TextMuted}"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </RadioButton>

            <!-- Error -->
            <TextBlock x:Name="lblError" FontSize="12"
                       Foreground="{DynamicResource AccentRed}"
                       Visibility="Collapsed" Margin="0,8,0,0"
                       TextWrapping="Wrap"/>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button Content="取消"
                        Style="{StaticResource CardBtn}"
                        Height="30" FontSize="12" Padding="14,3"
                        Margin="0,0,8,0" IsCancel="True"/>
                <Button Content="继续"
                        Style="{StaticResource PrimaryBtn}"
                        Height="30" FontSize="12" Padding="14,3"
                        Click="BtnContinue_Click"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>
